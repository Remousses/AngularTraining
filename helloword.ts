let message: string = 'Hello World';
console.log(message);

const isDone: boolean = false; // type primitif
const isDoneObject: Boolean = new Boolean(false); // objet Boolean

console.log(isDone)
console.log(isDoneObject)
